-- [결과]
-- 2^3 = 8
-- I I   O
-- a_exponent가 음수이면 값을 0으로 변경하고, 1을 반환
create or replace compute_pow(
	a_num IN number,
	b_exponent IN OUT number,
	a_pow OUT number
)
IS
BEGIN
	IF THEN 
	END IF;
	
END; 